name: google_plus_code
description:  Open Location Codes are short, generated codes that can be used like street addresses, for places where street addresses don't exist.
version: 0.0.1
homepage: anggacode.com

environment:
  sdk: '>=2.19.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

